import { auth, firestore, register_artist, register_company, register_user, storage } from "./firebase";

export {
    register_artist,
    register_user,
    register_company,
    firestore,
    auth,
    storage
}